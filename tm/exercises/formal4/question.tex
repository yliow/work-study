\tinysidebar{\debug{exercises/{formal4/question.tex}}}
Let $M$ be a TM with a set of accept states and a set of reject states.
Design another TM $M'$ with
exactly one accept state and exactly reject
such that
$L(M) = L(M')$.
