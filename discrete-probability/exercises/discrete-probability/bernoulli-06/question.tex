%-*-latex-*-
\tinysidebar{\debug{exercises/discrete-probability/{bernoulli-06/question.tex}}}
  What is the expected number of flips of a coin that
  shows a head with probability $p$ if we want to get $k$ heads?
