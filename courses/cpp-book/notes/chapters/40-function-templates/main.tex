\newpage\EMPHASIZE{40. Function Templates}

\textsc{Objectives}
\begin{itemize}
\item Write function templates
\item Make template function instantiation
\item Write function template specialization
\item Write header file for templates
\end{itemize}

We now come to a very important area in modern C++ programming. The
concept of templates. There are three types of templates

\begin{itemize}
\item Function templates
\item Struct templates
\item Class templates
\end{itemize}

For this chapter I'll focus on function templates.

C++ template programming is a very important technique in C++
programming because templates produces code. When you are writing C++
templates, you are not just writing code -- you are writing code that
produces code.

Modern C++ libraries are frequently written using templates. This is the
case for general algorithms and data structures, scientific computation,
computer vision, linear algebra, AI, etc.

This is only going to be a short introduction to function templates. C++
templates is a very vast subject, with the C++ standards committee
adding new features to templates every few years.

OK. Let's go.


\newpage\EMPHASIZE{An example of a function template}

Function templates are great ... really. It's perfect
for people who want to write less code. Let's try an
example.

Run this program.

\begin{consolethree}[escapeinside=||]
#include <iostream>

void println(int x)
{
    std::cout << x << std::endl;
}

int main()
{
    println(1);
    println(3.14);
    println('c');
    return 0;
}
\end{consolethree}

When you call println(3.14), C++ will try to find a function that best
fit the value passed in (i.e. a \texttt{double}). The closest is our
\texttt{println()} function but it accepts as \texttt{int}. So C++ typcast
the \texttt{double} 3.14 to an \texttt{int}, i.e. 3, and call println().

Let's say we do not want C++ to typecast. So
here's the program we want:

\begin{consolethree}[escapeinside=||]
#include <iostream>

void println(int x)
{
    std::cout << x << std::endl;
}

void println(double y)
{
    std::cout << y << std::endl;
}

void println(char z)
{
    std::cout << z << std::endl;
}

int main()
{
    println(1);
    println(3.14);
    println('c');
    return 0;
}
\end{consolethree}

Boy ... what a pain!!! Furthermore the functions all look pretty
similar. Smart people hate to do silly things like this: mindless
duplication of code.

OK. Before you swear at C++, rewrite this program like this:

\begin{consolethree}[escapeinside=||]
#include <iostream>

|\EMPHASIZE{template < typename T >}|
void println(|\EMPHASIZE{T}| x)
{
    std::cout << x << std::endl;
}

int main()
{
    println|\EMPHASIZE{< int >}|(1);
    println|\EMPHASIZE{< double >}|(3.14);
    println|\EMPHASIZE{< char >}|('c');
    return 0;
}
\end{consolethree}

WOW!!! There is only one function and it seems to work for all three
cases!!!

The ``function''

\begin{consolethree}[escapeinside=||]
template < typename T >
void println(T x)
{
    ...
}
\end{consolethree}

is called a \EMPHASIZE{function template}. It's actually
not a function. It's like a rubber stamp for producing
functions. You should think of the \EMPHASIZE{T} as a \EMPHASIZE{type variable} for the compiler. Here's how the compiler use
this \texttt{T}. When the compiler see this statement in your program:

\begin{consolethree}[escapeinside=||]
...
println|\textbf{< int >}|(1);
...
\end{consolethree}

it (i.e., the compile) says: ``Aha! So you need
\texttt{println\EMPHASIZE{< int >}}.
OK I'll take the rubber stamp \texttt{println} (i.e. the
function template) and create one with \EMPHASIZE{T} replaced by
\EMPHASIZE{int}.''

In other words C++ will insert this into the program:

\begin{consolethree}[escapeinside=||]
...
void println(|\EMPHASIZE{int}| x)
{
    std::cout << x << std::endl;
}
...
\end{consolethree}

It might be better to think of the instantiated function as:

\begin{consolethree}[escapeinside=||]
|\textbf{template <>}|
void println(|\textbf{int}| x)
{
    std::cout << x << std::endl;
}
\end{consolethree}

This ``production of a C++ function'' by a function template is called a
\EMPHASIZE{function instantiation}. Note that if
\texttt{println< char * >()} was not called, the
following function will \EMPHASIZE{not} appear in your program:

\begin{consolethree}[escapeinside=||]
...
|\textbf{template <>}|
void println(char * x)
{
    std::cout << x << std::endl;
}
...
\end{consolethree}

Note that the function template itself is actually not included in the
binary executable code that is executed. It's only used
as a rubber stamp by the compiler for creating actual functions.

Writing function templates is sort of like writing \EMPHASIZE{code that \underline{produces} code}. Specifically a function template can produce functions. This style of programming is called \EMPHASIZE{generic programming}. Templates also lead to something called \EMPHASIZE{metaprogramming}, but I won't get into that because that's beyond the scope of basic programming.

Do you see the power of function templates?

The variable type \texttt{T} (usually called a \EMPHASIZE{type parameter}) can appear anywhere in the function template, not just in the function header. Below is an example where the type parameter appears as a return type.

Another thing to remember is that you can use any identifier name for the type parameter. I used \texttt{T}. You can use \texttt{X} if you like. But the practice is to use a single uppercase letter.

\input{../exercises/0-40-function-templates/question.tex}
\input{../exercises/1-40-function-templates/question.tex}
\input{../exercises/2-40-function-templates/question.tex}

\newpage\EMPHASIZE{Another Example}

Here's a more substantial example. Recall this:

\begin{consolethree}[escapeinside=||]
#include <iostream>

bool arrayEqual(int[], int, int[], int);

int main()
{
    int x[] = {1, 2, 3};
    int y[] = {1, 2, 3};
    std::cout << arrayEqual(x, 3, y, 3);
    return 0;
}

bool arrayEqual(int x[], int xSize, int y[], int ySize)
{
    if (xSize == ySize)
    {
        bool same = true;
        for (int i = 0; i < xSize; i++)
        {
            if (x[i] != y[i])
            {
                same = false;
                break;
            }
        }
        return same;
    }
    else
    {
        return false;
    }
}
\end{consolethree}

What if you want to compare arrays of doubles? You need to have another
function:

\begin{consolethree}[escapeinside=||]
bool arrayEqual2(double x[], double y[])
{
    bool same = true;
    for (int i = 0; i < xSize; i++)
    {
        if (x[i] != y[i])
        {
            same = false;
            break;
        }
    }
    return same;
}
\end{consolethree}

Uh oh ... what about comparing array of booleans? Or characters? Etc.

Now try \textbf{\textit{this}}:

\begin{consolethree}[escapeinside=||]
#include <iostream>

|\EMPHASIZE{template < typename T >}|
|\EMPHASIZE{bool arrayEqual(T[], int, T[], int);}|

int main()
{
    int x[] = {1, 2, 3};
    int y[] = {1, 2, 3};
    std::cout << arrayEqual|\textbf{< int >}|(x, 3, y, 3)
              << std::endl;

    double a[] = {1.2, 3.4, 5.6};
    double b[] = {2.1, 4.3, 6.5};
    std::cout << arrayEqual|\EMPHASIZE{< double >}|(a, 3, b, 3)
              << std::endl;
    return 0;
}

|\EMPHASIZE{template < typename T >}|
bool arrayEqual(|\textbf{T}| x[], int xSize, |\textbf{T}| y[], int ySize)
{
    if (xSize == ySize)
    {
        bool same = true;
        for (int i = 0; i < 3; i++)
        {
            if (x[i] != y[i])
            {
                same = false;
                break;
            }
        }
        return same;
    }
    else
    {
        return false;
    }
}
\end{consolethree}

In this example our function template is this:

\begin{consolethree}[escapeinside=||]
template < typename T >
bool arrayEqual(T x[], int xSize, T y[], int ySize)
{
    ...
}
\end{consolethree}

Note that in the body of the function template, x and y are array of
elements of type T. The elements of the two arrays appear in the code as

\begin{consolethree}[escapeinside=||]
bool arrayEqual(T x[], int xSize, T y[], int ySize)
{
    ...
    |\EMPHASIZE{if (x[i] != y[i])}|
    ...
}
\end{consolethree}

As long as the \EMPHASIZE{!=} makes sense for type \EMPHASIZE{T}, C++ will be able to generate the actual function.

For instance you can compare characters using !=. Therefore you can

\begin{consolethree}[escapeinside=||]
...
|\EMPHASIZE{char}| s[] = {'a', 'b', 'c'};
|\EMPHASIZE{char}| t[] = {'a', 'b', 'c'};
std::cout << arrayEqual< |\EMPHASIZE{char}| >(x, 3, y, 3)
          << std::endl;
...
\end{consolethree}

\input{../exercises/3-40-function-templates/question.tex}
\input{../exercises/4-40-function-templates/question.tex}

\newpage\EMPHASIZE{Template parameters}

In all previous examples we have only one parameter. And the parameter
is used as a type. You can have as many template parameters as you like.
You can actually use value parameters. Try these examples.

\begin{consolethree}[escapeinside=||]
#include <iostream>

template < typename T, typename S >
void spam(T x, S y)
{
    std::cout << x << ", " << y << std::endl;
}

int main()
{
    spam< int, double >(1, 2.2);
    spam< double, int >(1.1, 2);
    spam< double, double >(1.1, 2.2);
    spam< int, int >(1.1, 2.2);
    return 0;
}
\end{consolethree}

(Of course for the last function there is a typecast from the two
doubles 1.1 and 2.2 into integers).

And here's an example where a template parameter is actually is an \EMPHASIZE{int} value:

\begin{consolethree}[escapeinside=||]
#include <iostream>

template < typename T, |\EMPHASIZE{int}| S >
T eggs(T x)
{
    return x + S;
}

int main()
{
    std::cout << eggs< int, 1 >(42) << std::endl;
    std::cout << eggs< int, 11 >(42) << std::endl;
    std::cout << eggs< double, 1 >(2.2) << std::endl;
    std::cout << eggs< char, 3 >('a') << std::endl;
    return 0;
}
\end{consolethree}

So now you know that a template parameter is declared with the word \EMPHASIZE{``typename''} if it is a type and if it is an integer value you use the word \EMPHASIZE{``int''}.

\input{../exercises/5-40-function-templates/question.tex}
\input{../exercises/6-40-function-templates/question.tex}
So now you know you can have either this

\begin{consolethree}[escapeinside=||]
template < typename T>
int max(T x[], int size)
{
    ...
}
\end{consolethree}

Or this:

\begin{consolethree}[escapeinside=||]
template < typename T, int SIZE >
int max(T x[])
{
    ... SIZE appears in code ...
}
\end{consolethree}

The big question is ... what's the difference between the two? Why
does C++ allow integer template parameters?

Well for the first version, an example of a function call is this:
\begin{console}
max< int >(x, 10);
\end{console}
For the second version , an example of a function call is this:

\begin{console}
max< int, 10 >(x);
\end{console}

In the second case, the \EMPHASIZE{int 10} is not passed into the function.

Review the \EMPHASIZE{very important} pages in the first chapter on
functions where I did a trace. When you call a function, in your CPU,
the program has to put the arguments for a function call into the
function call stack. The first version has to put two values into the
stack. The second version has to put one value into the stack:

\begin{consolethree}[escapeinside=||]
max< int >(|\EMPHASIZE{x}, \EMPHASIZE{10}|);
\end{consolethree}

\begin{consolethree}[escapeinside=||]
max< int, 10>(|\EMPHASIZE{x}|);
\end{consolethree}

See it? This is the benefit.

However there's a disadvantage. Recall that if you make the following
function call in your code:

\begin{console}
max< int, 10 >(x);
\end{console}

then your compiler will instantiate (i.e., create) the functions

\begin{consolethree}[escapeinside=||]
template <>
int max< int, 10 >(int x[])
{
    ...
}
\end{consolethree}

If you also have

\begin{console}
max< double, 20 >(y);
\end{console}

then your compiler will also instantiate

\begin{consolethree}[escapeinside=||]
template <>
int max< double, 10 >(int x[])
{
    ...
}
\end{consolethree}

This means that the second version will generate a large executable
binary code.

\input{../exercises/7-40-function-templates/question.tex}
\input{../exercises/8-40-function-templates/question.tex}
\input{../exercises/9-40-function-templates/question.tex}

\newpage\EMPHASIZE{Header Files}

Here's a very common gotcha. (I see this all the time.)

When you organize your template functions, you should remember to
include the \EMPHASIZE{body of the function template in the header file} not a cpp file. That's because function templates are inline functions.

\input{../exercises/10-40-function-templates/question.tex}

\newpage\EMPHASIZE{Automatic resolution}

Sometimes you don't have to tell C++ which ``version''
of the function to use. Here's a previous exercise:

\begin{consolethree}[escapeinside=||]
#include <iostream>

template < typename T >
void println(T x)
{
    std::cout << x << std::endl;
}

int main()
{
    println< int >(1);
    println< double >(3.14);
    println< char >('c');
    return 0;
}
\end{consolethree}

Modify and run this instead:

\begin{consolethree}[escapeinside=||]
#include <iostream>

template < typename T >
void println(T x)
{
    std::cout << x << std::endl;
}

int main()
{
    println(1);
    println(3.14);
    println('c');
    return 0;
}
\end{consolethree}

You see, when you call

\verb!println(1);!

this matches the version with

\begin{consolethree}[escapeinside=||]
template <>
void println(int x)
{
    std::cout << x << std::endl;
}
\end{consolethree}

and deduce that the T is int.

\input{../exercises/11-40-function-templates/question.tex}

\newpage\EMPHASIZE{Gotcha}

You notice that when I define a template function I write this:

\begin{consolethree}[escapeinside=||]
template < typename T >
bool arrayEqual(T x[], int xSize, T y[], int ySize)
{
    ...
}
\end{consolethree}

which is really the same as this:

\begin{consolethree}[escapeinside=||]
template <typename T>
bool arrayEqual(T x[], int xSize, T y[], int ySize)
{
    ...
}
\end{consolethree}

In other words there is no space after < and before
>. You should follow the first format. Why? Because, besides
functions, several other ``things'' can be ``templatized'', including
types. You can end up calling a template function with a template type
like this:

\verb!someFunction< someType< int > >(...);!

(Don't worry too much about this: I'll be talking about type templates
later.) So what will happen is that if you don't have a
practice of putting spaces around a type parameter? You might end up
with code like this:

\verb!someFunction<someType<int>>(...);!

And now you have a problem because the >> as in

\texttt{someFunction<someType<int\EMPHASIZE{>>}(...);}

is the input operator!!! (Also known as the stream insertion operator.)
To make matters worse, the error message you get in such cases tend to
be unintelligible.

If you know what you're doing you may leave out the
spaces. Otherwise it's best to stay with the first
format: Always have a space after < and before >
when used in the context of templates.

(ASIDE: The new C++0x standard, published since 2011, stipulates that a
2011-compliant C++ compiler should parse nested templates correctly. So
something like \texttt{someFunction<someType<int\EMPHASIZE{>>}(...)} should be fine for such a compiler. But to be absolutely safe, I suggest you follow the practice as stated above and use a space to separate the two \EMPHASIZE{>>}. That way your C++ code will compile on both new and old compilers.)


\newpage\EMPHASIZE{Non-deducible context and ambiguity}

There are times when your C++ compiler won't be able to deduce the
correct type for your template parameter T.

Try to compile this:

\begin{consolethree}[escapeinside=||]
template < typename T >
T f(int x)
{
    return x;
}

int main()
{
    f(42);
    return 0;
}
\end{consolethree}

In this case, T can be \EMPHASIZE{int}, But note that it can also be
\EMPHASIZE{double} since

C++ allows automatic typecasting from \EMPHASIZE{int} to
\EMPHASIZE{double}.

In these cases, you have to explicit specify the substitution for
\EMPHASIZE{T}:

\begin{consolethree}[escapeinside=||]
template < typename T >
T f(int x)
{
    return x;
}

int main()
{
    f|\EMPHASIZE{< int >}|(42);
    return 0;
}
\end{consolethree}


\newpage\EMPHASIZE{Template specialization}

There are times when you want ``special cases'' for your function
templates. For instance look at this example:

\begin{consolethree}[escapeinside=||]
#include <iostream>

template < typename T, int SIZE >
T sum(T x[])
{
    T sum = 0;
    for (int i = 0; i < SIZE; i++)
    {
        s += x[i];
    }
    return s;
}

int main()
{
    int x[10] = {3, 1, 5, 6, 2, 8, 9, 3, -2, 3};
    std::cout << sum< int, 1 >(x) << std::endl;
    std::cout << sum< int, 2 >(x) << std::endl;
    std::cout << sum< int, 10 >(x) << std::endl;

    double y[8] = {3.1, 1.8, 5.2, 6.7,
                   8.4, 9.1, 3.6, -2.7};
    std::cout << sum< double, 1 >(y) << std::endl;
    std::cout << sum< double, 2 >(y) << std::endl;
    std::cout << sum< double, 10 >(y) << std::endl;
    return 0;
}
\end{consolethree}

The function template sum does work. But clearly when the number of
terms to add is small, it's silly waste of time to do the summation in a
loop. For instance when the size is 1, you can just return x[0].
When the size is 2, it's x[0] + x[1]. In both of these cases, if
you unroll the for-loop, you would save on declaring int variable i with
initialization of 0, you save on checking i < size, you save
on ++i. There's also a cost in the CPU of going from the bottom of the
loop to the top of the loop. (This will be clarified in CISS360.) For
the case of size = 1, using a for-loop will make the function call about
5 times slower!

C++ allows you to specify special cases for a function template. These are called \EMPHASIZE{template specializations}.

Run this:

\begin{consolethree}[escapeinside=||]
#include <iostream>

template < typename T, int SIZE >
T sum(T x[])
{
    ...
}

|\EMPHASIZE{template <>}|
|\EMPHASIZE{int sum< int, 0 >(int x[])}|
|\EMPHASIZE{\{}|
    |\EMPHASIZE{return x[0];}|
|\EMPHASIZE{\}}|

int main()
{
    ...
}
\end{consolethree}

I've added a specialization of the \EMPHASIZE{sum} function template for the case when \EMPHASIZE{T = int} and \EMPHASIZE{SIZE = 0}.

\input{../exercises/12-40-function-templates/question.tex}
The above specialization is called a \EMPHASIZE{full specialization}. For
templates, there's a concept of \EMPHASIZE{partial specialization}. The
syntax would be something like this:

\begin{consolethree}[escapeinside=||]
\EMPHASIZE{template < typename T >}|
\EMPHASIZE{T sum< T, 0 >(int x[])}|
\EMPHASIZE{\{}|
    \EMPHASIZE{return x[0];}|
\EMPHASIZE{\}}|
\end{consolethree}

i.e., only \texttt{SIZE} has been specialized to \texttt{0}. However the above \EMPHASIZE{does not compile} because \EMPHASIZE{currently partial specialization is not supported for function templates}. (However it is supported for struct templates and class templates -- see later chapters for struct and classes.)

This is a bummer since if we do have partial specialization for function
templates, we'll be writing less code.

Also, note that the specialized function

\begin{consolethree}[escapeinside=||]
template <>
int sum< int, 0 >(int x[])
{
    return x[0];
}
\end{consolethree}

Is not a template anymore. It's a regular function. Therefore for multi-file compilation,

\begin{itemize}
\item You can put that in a cpp file and put the prototype in the header
  file, or
\item You can inline the function and put the above in the header file.
\end{itemize}

The standard practice for creating specializations is to create them for frequently used cases where you want speed. For instance, you probably do NOT want to have specializations for \verb!int sum< int, 0 >, int sum< int, 1 >, int sum< int, 2 >, ..., int sum< int, 99 >!!!!

\input{../exercises/13-40-function-templates/question.tex}
\input{../exercises/14-40-function-templates/question.tex}
\input{../exercises/15-40-function-templates/question.tex}
\input{../exercises/16-40-function-templates/question.tex}
\input{../exercises/17-40-function-templates/question.tex}
\input{../exercises/18-40-function-templates/question.tex}
\input{../exercises/19-40-function-templates/question.tex}