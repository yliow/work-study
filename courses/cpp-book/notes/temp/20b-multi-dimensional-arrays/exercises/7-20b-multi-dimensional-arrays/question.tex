\tinysidebar{\debug{exercises/{test/question.tex}}}
\begin{ex}
Declare an array of 5-by-10 integers. Assign random integers from 1 to 10 to the array. Print the array in a 5-by-10 grid of values separating values in each row by a space and separating each row with a newline. Prompt the user for a row and print the average of the values in that row. Repeat this exercise but let the user specify a
\texttt{\textbf{column}}.
\end{ex}
