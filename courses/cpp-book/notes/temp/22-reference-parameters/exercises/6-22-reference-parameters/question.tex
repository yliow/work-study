\tinysidebar{\debug{exercises/{6-22-reference-parameters/question.tex}}}Write an \texttt{arrayConcat()} function (concatenation of arrays) that appends an array to another. In other words, if \texttt{x} is the array \texttt{\{1, 2, 3\}} and \texttt{y} is the array \texttt{\{6, 7, 8, 9\}}, after calling this function with \texttt{x} and \texttt{y}, you want \texttt{x} to be \texttt{\{1, 2, 3, 6, 7, 8, 9\}}.



\begin{consolethree}[escapeinside=||]

#include <iostream>



void println(int x[], int len)

{

    for (int i = 0; i < len; i++)

    {

        std::cout << x[i] << ' ';

    }

    std::cout << std::endl;

}



void arrayConcat(int x[], int & xlen,

                 int y[], int ylen)

{

    ... CODE ...

}



int main()

{

    int x[100] = {1, 2, 3};

    int xlen = 3;

    int y[100] = {6, 7, 8, 9};

    int ylen = 4;

    arrayConcat(x, xlen, y, ylen);

    println(x, xlen);

    return 0;

}

\end{consolethree}

