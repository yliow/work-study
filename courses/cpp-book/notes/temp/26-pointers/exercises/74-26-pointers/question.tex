\tinysidebar{\debug{exercises/{74-26-pointers/question.tex}}}Write one statement that will change the value of \texttt{**p} by incrementing it with the value of \texttt{**q}. Use \texttt{p} and \texttt{q} and not hard-coded constants.

