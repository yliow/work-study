\begin{python0}
from solutions import *; clear()
\end{python0}
\sectionthree{What can you do with a function pointer?}

So what can you do with a function pointer? Well ... you can use it
just like a function:

\begin{consolethree}[escapeinside=||]
#include <iostream>

double avg(int x, int y)
{
    return (x + y) / 2.0;
}

int main()
{
    std::cout << avg(1, 2) << std::endl;
    std::cout << (unsigned int) avg << std::endl;
    double (*g)(int, int);
    g = &avg;
    std::cout << |\EMPHASIZE{g(1, 2)}| << std::endl;
    std::cout << (unsigned int) g << std::endl;
    return 0;
}
\end{consolethree}

\input{exercises/2-28-pointers-and-functions/main.tex}

