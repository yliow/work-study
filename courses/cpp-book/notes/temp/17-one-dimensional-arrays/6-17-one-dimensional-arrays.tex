\begin{python0}
from solutions import *; clear()
\end{python0}
\sectionthree{Max and min of an array of values}

\input{exercises/10-17-one-dimensional-arrays/main.tex}Neat isn't it?

Of course you can also compute the minimum:

\begin{consolethree}[escapeinside=||]
double a[] = {5.4, 1.2, 5.9, 3.14, 2.718};

double min = a[0];

for (int i = 1; i < 5; ++i)
{
    if (min > a[i]) min = a[i];
    {
        std::cout << min << std::endl;
    }
} 
\end{consolethree}

