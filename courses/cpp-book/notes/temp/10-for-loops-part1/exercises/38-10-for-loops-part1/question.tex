\tinysidebar{\debug{exercises/{38-10-for-loops-part1/question.tex}}} Modify the above program to compute the sum of all

integers from 1 to 100. You want to remove the pause in the middle of

the program:

\begin{console}[commandchars=\~\@\$]

int sum = 0;

for (int i = 1; i <= 3; i++)

{   

    ~sout@char c;$

    ~sout@std::cout << "enter something to continue ...";$

    ~sout@std::cin >> c;$

    

    sum = sum + i;

    ~textbf@std::cout << i << ' ' << sum << std::endl;$

}

std::cout << sum << std::endl;

\end{console}

