\tinysidebar{\debug{exercises/{0-70-cpp-string-class/question.tex}}}Do an experiment to verify that \texttt{operator>>} (i.e., input) is overloaded to work with \texttt{std::string} objects. Note that a whitespace will terminate an input.

