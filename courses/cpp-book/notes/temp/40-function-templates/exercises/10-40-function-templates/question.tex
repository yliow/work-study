\tinysidebar{\debug{exercises/{10-40-function-templates/question.tex}}}Write a header file \texttt{array.h} so that the following

works:



\begin{consolethree}[escapeinside=||]

#include "array.h"



int main()

{

    int x[] = {5, 3, 1, 2, 4};

    int xSize = sizeof(x) / sizeof(int);

    int y[] = {5, 3, 1, 2, 4};

    int ySize = sizeof(x) / sizeof(int);



    println< int >(x, xSize);

    println< int >(y, ySize);

    bubbleSort< int >(x, xSize);

    println< int >(x, xSize);

    println< int >(y, ySize);



    double a[] = {5.5, 3.3, 1.1, 2.2, 4.4};

    int aSize = sizeof(a) / sizeof(double);

    double b[] = {5.5, 3.3, 1.1, 2.2, 4.4};

    int bSize = sizeof(b) / sizeof(double);



    println< double >(a, aSize);

    println< double >(b, bSize);

    bubbleSort< double >(a, aSize);

    println< double >(a, aSize);

    println< double >(b, bSize);

    return 0;

}

\end{consolethree}

