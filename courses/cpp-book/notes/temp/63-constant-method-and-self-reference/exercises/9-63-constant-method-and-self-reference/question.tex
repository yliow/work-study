\tinysidebar{\debug{exercises/{test/question.tex}}}
\begin{ex}
Continuing the above idea, look at \texttt{IntArray}
and \texttt{IntDynArr}. There is a similar issue for
\texttt{operator[](int)}. Should it be a constant operator?
\end{ex}
