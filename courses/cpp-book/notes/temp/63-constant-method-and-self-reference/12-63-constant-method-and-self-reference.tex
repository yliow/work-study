\begin{python0}
from solutions import *; clear()
\end{python0}
\sectionthree{Notes}

In some programming languages, instead of \texttt{this} (in a method)
which points to the object invoking the method, in other programming
languages there's a \texttt{self} or \texttt{me} in a method
this plays the same role as \texttt{this} except that it is a reference to
the object invoking the method.


