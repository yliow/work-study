\tinysidebar{\debug{exercises/{test/question.tex}}}
\begin{ex}
Write a function \texttt{average()} so that \texttt{average(a, b, c)} returns the average of doubles \texttt{a}, \texttt{b}, and \texttt{c}.
\end{ex}
