\tinysidebar{\debug{exercises/{test/question.tex}}}
\begin{ex}
Write a function \texttt{prettyprintint()} such that \texttt{prettyprintint(n)} prints \texttt{n} with commas separating groups of 3 digits. For instance \texttt{prettyprintint(12345678)} prints \texttt{12,345,678} (note the commas).
\end{ex}
