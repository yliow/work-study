\tinysidebar{\debug{exercises/{5-64-static-members/question.tex}}}Modify the above \texttt{Date} class so that the constructor

uses the \texttt{default\_date\_} during the constructor and it uses an

initializer list.

