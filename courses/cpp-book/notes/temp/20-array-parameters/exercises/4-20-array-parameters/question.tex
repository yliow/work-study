\tinysidebar{\debug{exercises/{test/question.tex}}}
\begin{ex}
Write \texttt{reversefind()} function such that \texttt{reversefind(x, target, start, end)} will return the index of the value of \texttt{target} in array \texttt{x}, scanning right-to-left from index value \texttt{end - 1} to index value \texttt{start}. If the value is not found, \texttt{-1} is returned.
\end{ex}
