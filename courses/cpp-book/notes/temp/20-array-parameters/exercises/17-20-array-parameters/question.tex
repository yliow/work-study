\tinysidebar{\debug{exercises/{test/question.tex}}}
\begin{ex}
Write a function \texttt{array\_assign()} (for
``assignment operator'') such that
\texttt{array\_assign(x, x\_len, y, y\_len)}

will copy the first \texttt{y\_len} values in the array \texttt{y} to the array \texttt{x and set x\_len} to \texttt{y\_len.} Test your function.
\end{ex}
