\tinysidebar{\debug{exercises/{test/question.tex}}}
\begin{ex}
Write \texttt{find\_from()} function such that \texttt{find\_from(x, target, start, end)} returns the index of the value of \texttt{target} in array \texttt{x}, scanning left-to-right from index value \texttt{start} to index value \texttt{end - 1}. If the value is not found, \texttt{-1} is returned.
\end{ex}
