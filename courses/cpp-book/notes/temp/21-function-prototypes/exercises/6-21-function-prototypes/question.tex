\tinysidebar{\debug{exercises/{6-21-function-prototypes/question.tex}}}Create a brand new project, copy \texttt{mymath.cpp}

and \texttt{mymath.h} to the new project folder. Add these files to your

project. Create a \texttt{main()} that calls \texttt{min()} and

\texttt{max()}.

