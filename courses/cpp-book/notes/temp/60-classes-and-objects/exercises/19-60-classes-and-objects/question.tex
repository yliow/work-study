\tinysidebar{\debug{exercises/{19-60-classes-and-objects/question.tex}}}Create a pointer \texttt{p} to a \texttt{Time} object, allocate memory for

\texttt{p}, and call all the available methods through \texttt{p}.

Deallocate memory at the end.

