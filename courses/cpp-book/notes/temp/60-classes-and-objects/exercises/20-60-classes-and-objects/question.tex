\tinysidebar{\debug{exercises/{20-60-classes-and-objects/question.tex}}}Create an array of 1000 pointers to \texttt{Time} objects, allocate

memory for all of them, set the first to time 00:00:00, the second to

00:00:01, etc. and print their values. Deallocate memory for every

pointer.

