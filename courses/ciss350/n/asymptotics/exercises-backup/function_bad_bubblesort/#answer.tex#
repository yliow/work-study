(\verb!exercises/function_bad_bubblesort/answer.tex!)

The worst case runtime of the given bubblesort is
$O(n^2 \times $ the time to execute \verb!swap!).
The runtime of \verb!swap! is $O(n)$.
Therefore the worse case runtime of the given bubblesort is
$O(n^3)$.
