\newpage
\section{Nasser}

\begin{consolethree}[escapeinside=||]
class Int
{
public:
    |\tikzmarknode{i}{Int (int x = 0)}||\sidebox[-1.25cm]{f}{\textnormal{1. Forced to have a default constructor.}}||\DrawArrow{f}{i}|   
        : x_(x) {}
    void set(int a)
    {
        x_ = a;
    }
private:                    
    int x_;
};
|\sidebox[-0.75cm]{t}{\textnormal{Important point: \EMPHASIZE{Two} methods called for each object to set the initial values.}}|
int main()
{               
    |\tikzmarknode{a}{Int a[10];}||\sidebox[-4.5cm]{c}{\textnormal{2. Construct each \texttt{a[i]} with default constructor.}}||\DrawArrow{c}{a}|
    for (int i = 0; i < 10; ++i)
        |\tikzmarknode{i}{a[i].set[i];}||\sidebox[0.5cm]{s}{\textnormal{3. set each object.}}||\DrawArrow{s}{i}|
    ... |\DrawArrow[red, dashed]{t}{c}||\DrawArrow[red, dashed]{t}{s}|
}
\end{consolethree}


%};!\sidebox[0.5cm]{i}{\textnormal{Important point: \EMPHASIZE{Two} methods called for each object to set the initial values.}}!
