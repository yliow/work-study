\tinysidebar{\debug{exercises/{regex7/question.tex}}}
Write down the regex $r$ for the language $L$ over $\Sigma = \{a, b\}$ 
such that 
\[
L(r) = \{w \in \Sigma^* \mid w \text{ contains $a^3$} \}
\]
