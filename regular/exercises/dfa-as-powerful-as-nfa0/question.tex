\tinysidebar{\debug{exercises/{dfa-as-powerful-as-nfa0/question.tex}}}
Here's an $\NFA$ for $\emptyset$ for $\Sigma = \{a,b\}$:
\input{stdout23.tex}
Convert it to a $\DFA$ using the subset construction.
