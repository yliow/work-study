\tinysidebar{\debug{exercises/{regex8/question.tex}}}
Write down the regex $R$ for the language $L$ over $\Sigma = \{a, b\}$ 
such that 
\[
L = \{w \in \Sigma^* \mid w \text{ contains exactly 3 $a$'s} \}
\]
