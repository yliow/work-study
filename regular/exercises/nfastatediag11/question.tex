\tinysidebar{\debug{exercises/{nfastatediag11/question.tex}}}
Draw a transition diagram of an NFA that accepts the language of
strings over $\Sigma = \{a,b\}$ that contains $abab$.
Compare with a DFA.
\vspace{2in}
