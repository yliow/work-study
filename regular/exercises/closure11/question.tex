\tinysidebar{\debug{exercises/{closure11/question.tex}}}
  Let $f : \{a,b,c\} \rightarrow \{0,1\}$, $f(a) = f(b) = 0$, $f(c) = 1$.
\itemize{
  \item Let $L$ be the set of strings with 3 $a$'s. What is $f^*(L)$?
  \item Let $L'$ be the set of strings with 3 1's. What is
    ${f^*}^{-1}(L')$?
  }
