\tinysidebar{\debug{exercises/{countability15/question.tex}}}
  Prove that $P(\N)$ is uncountable.
  This means $|\N| < |P(\N)|$.
  In fact even more is true:
  if $X$ is a set, then $|X| < |P(X)|$.
  (This is called Cantor's theorem and is extremely important.
  See later.)
